# OpenNutri Reference Server

Run `npm run demo`. The dependency-free server demonstrates a health endpoint,
authenticated event retrieval, subscription creation, and revocation with
in-memory sample data. It intentionally does not implement production identity,
OAuth, persistence, webhook delivery, rate limiting, or key management.

The literal `demo-token` is local demonstration data, not a deployable credential.
