# OpenNutri Swift SDK

`OpenNutri.swift` contains a Foundation-only Codable V1 model suitable for a
Swift package target. Configure `JSONDecoder.dateDecodingStrategy = .iso8601`
and validate schema semantics before trusting an event.
