# OpenNutri Python SDK

`opennutri.py` provides a dependency-free V1 envelope validator and typed result.
For complete production validation, pair it with a Draft 2020-12 JSON Schema
implementation and `schema/event.schema.json`.
